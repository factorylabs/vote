extends layout
block body
  .row
    .col-md-6.col-md-offset-3
      h3 Open Contests
      for contest in contests
        p: a(href="contests/#{contest.id}")= contest.name
      if contests.length == 0
        p No contests.
