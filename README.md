# FDL Employees Vote!

## Tech

Postgres

Node.js

* `coffeescript`
* `express`
* `bookshelf` + `knex`
* `jade` + `stylus`
* `knox` for S3 storage

## Notes

Vote doesn't clean up after itself in its S3 bucket.
